# Add these directives inside your existing location / block to avoid 504 Gateway Timeout
# and ERR_CONNECTION_CLOSED during long SSE imports (Import from Digital Menu Link).
#
# Default nginx proxy timeouts are often 60s; long operations can exceed that and nginx returns 504
# or closes the stream while the app is still extracting dishes.
# Increase as needed (e.g. 300s = 5 minutes). Reload after editing: sudo nginx -t && sudo systemctl reload nginx

        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;

# Your full location block should look like this (with the new lines added):

#    location / {
#        proxy_pass http://127.0.0.1:3016;
#        proxy_http_version 1.1;
#
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#        proxy_set_header X-Forwarded-Proto $scheme;
#
#        proxy_set_header Upgrade $http_upgrade;
#        proxy_set_header Connection "upgrade";
#
#        proxy_connect_timeout 300s;
#        proxy_send_timeout 300s;
#        proxy_read_timeout 300s;
#    }

# After editing, test and reload nginx:
#   sudo nginx -t && sudo systemctl reload nginx
